mysql:
    username: username
    password: password
    address: address
    port: 1234

redis:
    address: address
    password: password
    db: 0

key:
    jwtkey: jwtkey

oauth:
    github:
        id: "client id"
        secret: "client secret"
        redirect: "redirect url"